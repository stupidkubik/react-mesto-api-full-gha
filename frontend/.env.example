# Frontend runtime
# These variables are preparatory: current code still uses a hardcoded API host.
# They should become active in the frontend refactor phase.

REACT_APP_API_BASE_URL=http://localhost:3000
