# Frontend runtime
# The Vite baseline is already active, but the application still uses a hardcoded API host.
# This variable becomes active in Phase 4.3 when the API client is moved to env-driven config.

VITE_API_BASE_URL=http://localhost:3000
